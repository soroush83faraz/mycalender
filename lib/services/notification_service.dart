class NotificationService {
  static Future<void> initialize() async {
    // Notification service disabled for now
    print('Notification service initialized (disabled)');
  }

  static Future<void> scheduleEventReminder(dynamic event) async {
    // Notification service disabled for now
    print('Event reminder scheduled (disabled)');
  }

  static Future<void> cancelEventReminder(String eventId) async {
    // Notification service disabled for now
    print('Event reminder cancelled (disabled)');
  }
}
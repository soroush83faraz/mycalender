# راهنمای آپدیت آنلاین اپ (تقویم فارسی)

این پروژه دو مسیر مکمل برای آپدیت آنلاین دارد:

| مسیر | چه چیزی را آپدیت می‌کند | تجربهٔ کاربر | کِی استفاده کنیم |
|---|---|---|---|
| **۱. آپدیتر APK داخل اپ** (پیاده‌سازی‌شده) | کل اپ (نسخهٔ کامل) | نوتیف/دیالوگ → دانلود → نصب | هر تغییری، مخصوصاً وقتی کد نیتیو/پلاگین عوض شده |
| **۲. Shorebird (Code Push)** | فقط کد Dart/Flutter | خودکار و بی‌صدا موقع باز شدن اپ | رفع باگ سریع و تغییرات UI/منطق |

> چون اپ را **مستقیم (APK)** پخش می‌کنی، مسیر ۱ ستون فقرات آپدیت توست و مسیر ۲ برای وصله‌های سریع.

---

## مسیر ۱ — آپدیتر APK داخل اپ

### چطور کار می‌کند
1. اپ موقع باز شدن (و هر وقت کاربر در تنظیمات «بررسی به‌روزرسانی» را بزند) سند Firestore زیر را می‌خواند:
   - مجموعه: `app_config` / سند: `latest`
2. اگر `build` یا `version` جدیدتر از نسخهٔ نصب‌شده باشد، دیالوگ آپدیت با یادداشت‌های نسخه نشان داده می‌شود.
3. با زدن «به‌روزرسانی»، APK از `apk_url` دانلود می‌شود (با نوار پیشرفت) و نصب‌کنندهٔ سیستم باز می‌شود.

هیچ پکیج pub جدیدی اضافه نشده — از Firestore موجود، `dart:io` و یک MethodChannel کوچک (در `MainActivity.kt`) استفاده شده.

### قدم‌های انتشار یک آپدیت جدید

**۱) نسخه را در `pubspec.yaml` بالا ببر** (هر دو قسمت مهم‌اند):
```yaml
version: 1.0.1+2      #  ۱.۰.۱ = version ، ۲ = build (versionCode)
```

**۲) APK بساز:**
```bash
flutter build apk --release
# خروجی: build/app/outputs/flutter-apk/app-release.apk
```

**۳) APK را روی یک هاست در دسترس ایران آپلود کن** و لینک مستقیم (https) بگیر.
گزینه‌های خوب: سرور خودت، آبجکت‌استوریج ابر آروان/پارس‌پک، یا هر CDN داخلی.
(از گیت‌هاب/گوگل‌درایو استفاده نکن چون ممکن است برای کاربر ایرانی فیلتر باشد.)

**۴) سند Firestore را آپدیت کن** (کنسول Firebase → Firestore → مجموعهٔ `app_config` → سند `latest`):
```json
{
  "version": "1.0.1",
  "build": 2,
  "apk_url": "https://your-host.ir/downloads/app-1.0.1.apk",
  "notes": "رفع چند باگ و بهبود سرعت.",
  "mandatory": false
}
```
- `build` باید همان عددِ بعد از `+` در pubspec باشد.
- `mandatory: true` یعنی کاربر نتواند دیالوگ را ببندد (برای آپدیت‌های اجباری).

همین! دفعهٔ بعد که کاربر اپ را باز کند، آپدیت را می‌بیند.

### قانون امنیتی Firestore (لازم)
تا کاربران (حتی مهمان) بتوانند سند نسخه را بخوانند ولی نتوانند تغییرش دهند، این را به Firestore Rules اضافه کن:
```
match /app_config/{doc} {
  allow read: if true;      // خواندن برای همه آزاد
  allow write: if false;    // نوشتن فقط از کنسول/ادمین
}
```

### نکتهٔ مجوز نصب
اندروید ۸ به بالا برای نصب APK از داخل اپ، مجوز «نصب برنامه‌های ناشناس» می‌خواهد.
- این مجوز (`REQUEST_INSTALL_PACKAGES`) در `AndroidManifest.xml` اضافه شده.
- اگر کاربر هنوز اجازه نداده باشد، اپ خودکار او را به صفحهٔ تنظیمات می‌فرستد؛ بعد از فعال‌کردن، دوباره «به‌روزرسانی» را می‌زند.

### فایل‌هایی که این قابلیت را می‌سازند
- `lib/services/update_service.dart` — منطق چک/دانلود/نصب
- `lib/services/apk_downloader_io.dart` + `apk_downloader_stub.dart` — دانلود (سازگار با وب)
- `lib/widgets/update_dialog.dart` — دیالوگ و نوار پیشرفت
- `android/.../MainActivity.kt` — نصب از طریق FileProvider
- `AndroidManifest.xml` + `res/xml/file_paths.xml` — مجوز و FileProvider
- فراخوانی‌ها: `main_screen.dart` (چک خودکار) و `settings_screen.dart` (دکمهٔ دستی)

---

## مسیر ۲ — Shorebird (Code Push)

با Shorebird می‌توانی تغییرات **کد Dart** را بدون ساخت و پخش APK جدید، مستقیم به کاربرها برسانی.

### راه‌اندازی (یک‌بار)
```bash
# نصب CLI
curl --proto '=https' --tlsv1.2 https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh -sSf | bash
# (روی ویندوز: از دستور PowerShell موجود در shorebird.dev استفاده کن)

shorebird login          # نیاز به مرورگر و اکانت
shorebird init           # فایل shorebird.yaml را می‌سازد و app_id می‌دهد
```

### انتشار نسخهٔ پایه (به‌جای flutter build)
```bash
shorebird release android
```
این خروجی را که ساخت به کاربر بده (همین را روی هاستت بگذار).

### فرستادن یک وصلهٔ کد (Patch)
بعد از تغییر کد Dart:
```bash
shorebird patch android
```
کاربر دفعهٔ بعد که اپ را باز کند، وصله بی‌صدا دانلود و اعمال می‌شود.

### محدودیت‌ها
- فقط تغییر **کد Dart** پوشش داده می‌شود؛ تغییر کد نیتیو، افزودن/حذف پلاگین، تغییر assets یا بالا بردن `version` → باید APK کامل بدهی (همان مسیر ۱).
- iOS طبق قوانین اپل محدودتر است.

### ⚠️ نکتهٔ مهم برای کاربران ایران
سرورهای Shorebird روی زیرساخت گوگل/خارج هستند. اگر کاربرهای نهایی‌ات در ایران باشند و به این سرورها دسترسی نداشته باشند، **وصله‌ها ممکن است دانلود نشوند**.
- موقع build خودت (توسعه‌دهنده) هم احتمالاً به VPN نیاز داری.
- به همین دلیل **مسیر ۱ (آپدیتر APK) را مسیر اصلی و قابل‌اتکا در نظر بگیر** و Shorebird را فقط اگر دسترسی سرورهایش پایدار بود استفاده کن.

---

## نکتهٔ محیط توسعه: خطای ۴۰۳ در `flutter pub get`

روی این شبکه، endpointِ متادیتای pub.dev (`https://pub.dev/api/packages/*`) خطای **۴۰۳** می‌دهد (فیلترینگ ISP)، در حالی که دانلود آرشیو پکیج‌ها کار می‌کند. برای همین:
- تا وقتی پکیج **جدید** اضافه نکنی، اپ بدون مشکل build می‌شود (پکیج‌ها از قبل در `pubspec.lock` حل شده‌اند).
- برای افزودن هر پکیج جدید یا اجرای `flutter pub get` کامل، **VPN روشن کن** یا از یک mirror معتبر pub استفاده کن.
- به همین دلیل این آپدیتر عمداً **بدون هیچ پکیج جدید** نوشته شد.
